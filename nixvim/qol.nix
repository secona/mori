{ ... }:

{
  plugins.comment = {
    enable = true;
  };

  plugins.nvim-autopairs = {
    enable = true;
  };

  plugins.barbecue = {
    enable = true;
  };

  # TODO
  # vim-visual-multi

  plugins.tmux-navigator = {
    enable = true;
  };

  plugins.sleuth = {
    enable = true;
  };

  plugins.surround = {
    enable = true;
  };

  plugins.which-key = {
    enable = true;
    window.border = "single";
  };

  plugins.telescope = {
    enable = true;
    # TODO
    # keybinds
  };
}